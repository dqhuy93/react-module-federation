{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "fe-my-menu-host",
      "fe-my-menu-main",
      "fe-my-menu-booking"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.3"
  }
}
